# Localee — задачи

Живой чек-лист проекта. Лежит в гите → можно выгрузить и смотреть/править у себя.
Отмечай сделанное: `- [ ]` → `- [x]`. Добавляй идеи в конце.

_Обновлено: 2026-06-18_

---

## ✅ Сделано

### Бэкенд (папка `server/`, Node + Express + SQLite)
- [x] Каркас сервера, конфиг через `.env`, база-файл SQLite
- [x] Регистрация `POST /api/auth/register` (хеш пароля, JWT-токен)
- [x] Вход `POST /api/auth/login`
- [x] Профиль `GET /api/auth/me` (по токену)
- [x] Редактирование профиля `PATCH /api/auth/me`
- [x] Валидация ввода, единый JSON-обработчик ошибок (без утечки стека), 404 JSON
- [x] Прогон всех маршрутов проверен (13/13)
- [x] Роли пользователей (`role`: user/admin) + бутстрап админов через `ADMIN_EMAILS`
- [x] Админ-API `/api/admin/*`: список с поиском, статистика, смена роли, удаление (с защитой: не себя, не env-админа)

### Фронт (аккаунты)
- [x] Клиент API `src/utils/api.ts` (адрес из `VITE_API_URL`, токен в localStorage)
- [x] Ворота авторизации: не вошёл — приложение закрыто
- [x] Экран входа/регистрации `AuthScreen` (стилизован под тему)
- [x] Лендинг (встречная страница) `LandingPage`
- [x] Профиль показывает свои данные с сервера (`profile.ts`)
- [x] Редактирование профиля сохраняется на сервер
- [x] Выход из аккаунта
- [x] Сессия восстанавливается при перезагрузке
- [x] **Админ-панель** `AdminPage`: список пользователей, поиск, статистика, назначить/снять админа, удалить. Вход (Topbar + меню профиля) виден только роли admin

### Карта / UI
- [x] Кастомные фильтры на карте: мгновенное переключение + плавные анимации
- [x] Клик по логотипу «Localee» → на главную (карта)
- [x] Убрана вкладка «Все места»

---

## 🔜 Дальше (по приоритету)

- [ ] **Друзья**: поиск по нику, заявки (отправить/принять/отклонить), список, статус «онлайн»
- [x] **Чаты (личные)**: отправка/история, поиск людей для нового чата, «живой» опрос (3с). Группы и WebSocket — позже
- [x] **Деплой бэкенда** на VPS (reg.cloud); фронт смотрит на `https://api.localee.ru` (`.env.production`)
- [ ] **Достижения/места → на бэкенд** (сейчас в localStorage, общие для аккаунтов на одном устройстве)
- [ ] **AI-помощник → YandexGPT** через сервер (сейчас `claudeApi.ts` зовёт api.anthropic.com напрямую — в РФ не работает, ключ светится)

---

## 🧩 Доделать (есть кнопки-заглушки)

- [x] Загрузка фото аватара (выбор файла + обрезка до 256×256 + сохранение в профиль)
- [x] Сброс достижений (кнопка в «Опасной зоне» с подтверждением)
- [ ] Удаление аккаунта (кнопка в редактировании профиля)
- [ ] Сброс пароля и подтверждение email
- [ ] Вход через Yandex ID / VK (схема под это уже заложена — таблица `auth_identities`)
- [ ] Защита входа от перебора паролей (rate-limit)

---

## 💡 Идеи

- [ ] Избранное (сохранённые места)
- [ ] Маршрутизатор 2ГИС (строить маршрут по дорогам, не по прямой)
- [ ] Авто-деплой (GitHub Actions → FTP/SSH на reg.ru)
- [ ] Push-уведомления (новые сообщения, заявки в друзья)

---

## 🐞 Технический долг / заметки

- [ ] ESLint: пустые `catch {}` в `useAchievements.ts`; `setState` в `useEffect` в `useIsMobile.ts` и `ProfileMenu.tsx` (старый код, не критично)
- [ ] Перед прод-сборкой обязательно задать `VITE_API_URL`, иначе в бандл зашьётся `http://localhost:4000`
- [ ] `JWT_SECRET` на проде заменить на длинную случайную строку (в `.env`)
