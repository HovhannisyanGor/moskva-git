node_modules
# Именно /data, а не data: без слеша правило матчит ЛЮБУЮ папку с таким именем
# на любой глубине, и из-за этого в git не попадал src/data/places.js.
# Со слешем игнорируется только server/data — файл базы, как и задумано.
/data
.env
